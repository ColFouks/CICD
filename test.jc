imports = ["globalConfig.cfg", "buildConfig.cfg"]
common {
    github {
        org = "SomeOrg"
        repo = "JavaTest"
    }
    folder  {
        project = "Java"
    }
}

__build {
    imports = ["mavenBuild.cfg"]
    maven {
        steps = "deploy"
    }
}
Build {
    imports = ["__build"]
}
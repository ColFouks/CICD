imports = ["globalConfig.cfg", "buildConfig.cfg"]
common {
    folder {
        projectGroup = "Test"
        project = "First"
    }
    maven {
        setToSnapshot = true
    }
    github {
        org = "ColFouks"
        repo = "Java_test"
    }
}

__build {
    imports = ["mavenBuild.cfg"]
    maven {
        steps = "deploy"
    }
}
ORE_Build {
    imports = ["__build"]
}